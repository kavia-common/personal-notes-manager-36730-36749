# Example environment configuration for notes_backend

# Port for the FastAPI server (default 3001)
PORT=3001

# SQLAlchemy database URL (default: sqlite:///./notes.db)
# For SQLite local file:
# NOTES_DB_URL=sqlite:///./notes.db
# For Postgres example:
# NOTES_DB_URL=postgresql+psycopg2://user:password@hostname:5432/dbname
NOTES_DB_URL=sqlite:///./notes.db

# Set to "true" to enable uvicorn reload (development only)
UVICORN_RELOAD=false
