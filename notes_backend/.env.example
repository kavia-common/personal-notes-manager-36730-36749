# Port to run the FastAPI server on (default: 3001)
PORT=3001

# Set to true to enable uvicorn reload (development)
UVICORN_RELOAD=false

# SQLAlchemy database URL for notes storage
# Defaults to a local SQLite file at ./notes.db if not provided
# Example for Postgres: postgresql+psycopg2://user:password@localhost:5432/notesdb
NOTES_DB_URL=sqlite:///./notes.db
